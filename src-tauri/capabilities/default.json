{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default app capability for local EyeRest windows.",
  "windows": ["settings", "overlay-*"],
  "permissions": ["core:default"]
}
